pipeline {
    agent {
        label 'docker-node'  // This label should match the one configured for your Docker agent node
    }
    
    stages {
        stage('Build') {
            steps {
                script {
                    // Your build steps go here
                    echo "Running on Docker agent node"
                }
            }
        }
    }
}
